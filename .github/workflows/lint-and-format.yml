name: Lint & Format

on:
  push:
    branches: [main, staging]
  pull_request:
    branches: [main, staging]

jobs:
  lint-and-format:
    name: ESLint & Prettier
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Run ESLint
        id: eslint
        run: yarn lint
        continue-on-error: true

      - name: Annotate ESLint failure
        if: steps.eslint.outcome == 'failure'
        run: |
          echo "::error title=ESLint failed::Lint errors were found. Run 'yarn lint:fix' locally to auto-fix them, then commit the changes."
          exit 1

      - name: Run Prettier check
        id: prettier
        run: yarn format:check
        continue-on-error: true

      - name: Annotate Prettier failure
        if: steps.prettier.outcome == 'failure'
        run: |
          echo "::error title=Prettier check failed::Formatting issues were found. Run 'yarn format' locally to fix them, then commit the changes."
          exit 1
